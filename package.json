{
  "name": "flowlab",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check domain.js && node --check cloud.js && node --check media-store.js && node --check store.js && node --check flowlab-app.js"
  },
  "dependencies": {
    "express": "5.1.0"
  }
}
