{
  "name": "flowlab",
  "version": "3.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/domain.js && node --check src/cloud.js && node --check src/store.js && node --check src/app.js && node --check api/ai.js && node --check api/health.js && node --check scripts/preflight.mjs",
    "preflight": "node scripts/preflight.mjs",
    "serve": "node scripts/server.mjs"
  }
}
